@{
    ViewData["Title"] = "Home Page";
}

@model SportClubs1.Data.HomeViewModel

<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мережа Спортивних Клубів</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="~/css/Home.css" />
</head>
<body>
        <div class="container">
        <div class="image-overlay">
            <img src="https://kalushcity.gov.ua/assets/catalog/000/000/186/1618402657_932c47a8b0dbd62604a6_large.jpg" alt="Тренажерний зал">
            <div class="text-block">
                <h1>Тренуйся!</h1>
                <p>Понад  @Model.Gyms.Count() тренажерних залів<br>Неймовірний простір для досягнення фітнес-мрій</p>
                <a href="@Url.Action("Index", "Gyms")" class="btn button">Дізнатися більше</a>
            </div>
        </div>
        </div>

        <div id="carouselExample" class="carousel slide">
            <div class="carousel-inner">
                 @foreach (var item in Model.Gyms)
            {
                if (item == Model.Gyms.First())
                {
                    <div class="carousel-item active">
                        <img src="data:image /jpeg;base64,@Convert.ToBase64String(item.Image)" class="d-block w-100" alt="...">
                    </div>
                }
                else
                {
                    <div class="carousel-item">
                        <img src="data:image /jpeg;base64,@Convert.ToBase64String(item.Image)" class="d-block w-100" alt="...">
                    </div>
                }
            }
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
    </div>
    @* <div class="title">Більше @Model.Gyms.Count() спортзалів</div>
    <div class="card-group">
    @foreach (var item in Model.Gyms)
    {
    <div class="card">
    <img src="">
    <div class="card-body">
    <h5 class="card-title">@item.Name</h5>
    </div>
    </div>
    }
    </div> *@
        <div class="card-set">
            <h1>@Model.Staffs.Count() професіональних тренерів</h1>
            <div class="card-group">
                @foreach (var person in Model.Staffs)
                {
                    <div class="card">
                        @if (person.Pfp == null)
                        {
                            <img src="https://cdn.pixabay.com/photo/2015/10/05/22/37/blank-profile-picture-973460_960_720.png" alt="No profile picture" />
                        }
                        else
                        {
                            <img src="data:image/jpeg;base64,@Convert.ToBase64String(person.Pfp)" alt="~staffpfp/none.jpg">
                        }
                        <div class="card-body">
                            <h5 class="card-title">@person.Name @person.Sername</h5>
                            <p class="card-text">@person.Profession</p>
                            @* <a href="#" class="btn btn-primary">Go somewhere</a> *@
                        </div>
                    </div>
                }
            </div>
        </div>

        <div class="list-set">
            <h2>Найновіші тренажери</h2>
            <div class="card">
                <ul class="list-group list-group-flush">
                    @foreach (var item in Model.TrainingMachines)
                    {
                        <li class="list-group-item">@item.Name</li>
                    }
                </ul>
            </div>
        </div>

    <p>Долучайтеся самі, кличте друзів та знаходьте нових, адже наша мережа об'єднує @Model.Clients.Count() людей!</p>
</body>
</html>
